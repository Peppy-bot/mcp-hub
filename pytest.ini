[pytest]
# Collect from the whole checkout, dot-directories included: pytest's default
# skips them, which would hide a test under .github. Only the repository's
# metadata and a local virtual environment stay out.
norecursedirs = .git .pytest_cache .venv venv
